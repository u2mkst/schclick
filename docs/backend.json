
{
  "entities": {
    "School": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "School",
      "type": "object",
      "description": "Represents a school participating in the 'School Click' application, tracking its engagement score, ranking information, and descriptive details.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the School entity."
        },
        "name": {
          "type": "string",
          "description": "The official full name of the school, typically retrieved from an external API like NEIS."
        },
        "score": {
          "type": "number",
          "description": "The total number of clicks accumulated by the school, indicating its popularity and engagement level."
        },
        "daebakScore": {
          "type": "number",
          "description": "The total number of 'Dae-bak' clicks accumulated by the school, used for the Best Meal ranking."
        },
        "cityProvinceName": {
          "type": "string",
          "description": "The name of the city or province office of education (e.g., '서울특별시교육청') to which the school belongs, often retrieved from an external API."
        },
        "schoolKind": {
          "type": "string",
          "description": "The type or classification of the school (e.g., '고등학교', '중학교', '초등학교'), often retrieved from an external API."
        },
        "slogans": {
          "type": "array",
          "description": "A list of fun and unique slogans or taglines associated with the school, potentially generated by an AI tool to foster school spirit.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "name",
        "score",
        "cityProvinceName",
        "schoolKind"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/schools/{schoolId}",
        "definition": {
          "entityName": "School",
          "schema": {
            "$ref": "#/backend/entities/School"
          },
          "description": "Stores individual school data including its name, total clicks (score), city/province, school type, and AI-generated slogans. Documents in this collection are publicly readable and allow any user to increment the 'score' and 'daebakScore' fields.",
          "params": [
            {
              "name": "schoolId",
              "description": "The unique identifier for the school."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure for the 'School Click' application uses a flat /schools collection. Each document stores metadata and scores (score, daebakScore). This design ensures data clarity and supports Querying as Permissions (QAPs) for leaderboards."
  }
}
